# Real Word Desktop fixtures (local only)

Place your own Word-created test documents here for manual QA.

**Do not commit documents containing real colleague or client names.**

Recommended local file: `test_comment_track.docx` (gitignored).

Synthetic fixtures used in CI are generated by tests or committed with fictional author names only.
